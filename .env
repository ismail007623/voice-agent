# Leave blank to use the same origin (for example, when a reverse proxy exposes /voice-chat).
# Set this to the FastAPI server origin during separate local development.
VITE_API_BASE_URL=http://127.0.0.1:8000